Регистры портов:
	DDRxn -- определение порта где x - буква порта(какой порт), а n - номер порта
			например DDRC3
	PORTxn -- подаем высокий уровень сигнала на пин
	
	
Комманды:
	_delay_ms() - задержка программы на установленное кол-во милисек.
				  подключается команда библиотекой <util/delay.h>


Объявления:
	#define -- такой командой можно объявить что-то своё, например
			  #define three 3
			  
	#define F_CPU 1000000UL -- объявляем, что МК будет работать на
			частоте 1Мгц
